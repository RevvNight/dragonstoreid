/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.dragonstore.app',
  appName: 'Dragon Store',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

module.exports = config;
